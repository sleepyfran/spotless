import { Database } from "@spotless/data-db";

/**
 * Provides queries and mutations for the {{pascalCase name}} table in the database.
 */
export class {{pascalCase name}}Data {
  constructor(readonly db: Database) {}
}
