/**
 * Contains the routes for the application.
 */
export const Paths = {
  root: "/",
  artists: "/artists",
  albums: "/albums",
  genres: "/genres",
  auth: {
    root: "/auth",
    callback: "/auth/callback",
  },
};
